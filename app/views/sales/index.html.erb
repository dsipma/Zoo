<h2>All Sales</h2>
<table>
  <tr>
  <th>User </th>
  <th>Animal</th>
  <th>Time</th>
  </tr>
  <% @sales.each do |sale|%>
  <tr>
    <td><%=sale.user.name%></td>
    <td><%=sale.animal.species%></td>
    <td><%=sale.created_at.strftime("%b %d, %I:%M %p")%></td>
  </tr>
      <%end%>
</table>