<h2><%= @user.name%>'s Zoo</h2>

Animals:
<ul><%if @user.animals.length%>
	<%=@user.name%> doesn't currently have any animals.
    <%else%>
        <% @user.animals.each do |animal|%>
	        <li><%= link_to animal.species, animal_path(animal)%></li>
        <%end%>
  <%end%>

</ul>
